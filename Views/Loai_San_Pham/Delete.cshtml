@model Day09LabCF.Models.Loai_San_Pham

@{
    ViewData["Title"] = "Xóa lo?i s?n ph?m";
}

<h1 class="mb-4 text-danger fw-bold text-uppercase">
    <i class="fa-solid fa-trash-can me-2"></i> @ViewData["Title"]
</h1>

<div class="row justify-content-center">
    <div class="col-md-6">
        <div class="card shadow-lg border-0">
            <div class="card-header bg-danger text-white fw-semibold">
                <i class="fa-solid fa-triangle-exclamation me-2"></i> Xác nh?n xóa lo?i s?n ph?m
            </div>

            <div class="card-body">
                <div class="alert alert-warning d-flex align-items-center" role="alert">
                    <i class="fa-solid fa-circle-exclamation me-2 fa-lg"></i>
                    <div>
                        B?n có ch?c ch?n mu?n <strong>xóa</strong> lo?i s?n ph?m này không?  
                        <br />Hành ð?ng này không th? hoàn tác!
                    </div>
                </div>

                <dl class="row mb-0">
                    <dt class="col-sm-4 text-end text-secondary fw-semibold">
                        @Html.DisplayNameFor(model => model.MaLoai)
                    </dt>
                    <dd class="col-sm-8 fw-bold text-dark">
                        @Html.DisplayFor(model => model.MaLoai)
                    </dd>

                    <dt class="col-sm-4 text-end text-secondary fw-semibold">
                        @Html.DisplayNameFor(model => model.TenLoai)
                    </dt>
                    <dd class="col-sm-8 fw-bold text-dark">
                        @Html.DisplayFor(model => model.TenLoai)
                    </dd>

                    <dt class="col-sm-4 text-end text-secondary fw-semibold">
                        @Html.DisplayNameFor(model => model.TrangThai)
                    </dt>
                    <dd class="col-sm-8">
                        @if (Model.TrangThai)
                        {
                            <span class="badge bg-success px-3 py-2">
                                <i class="fa-solid fa-check-circle me-1"></i> Ho?t ð?ng
                            </span>
                        }
                        else
                        {
                            <span class="badge bg-danger px-3 py-2">
                                <i class="fa-solid fa-xmark-circle me-1"></i> Ng?ng ho?t ð?ng
                            </span>
                        }
                    </dd>
                </dl>
            </div>

            <div class="card-footer bg-light d-flex justify-content-between">
                <a asp-action="Index" class="btn btn-secondary">
                    <i class="fa-solid fa-arrow-left me-1"></i> Quay l?i
                </a>

                <form asp-action="Delete" method="post" class="d-inline">
                    <input type="hidden" asp-for="Id" />
                    <button type="submit" class="btn btn-danger">
                        <i class="fa-solid fa-trash-can me-1"></i> Xóa
                    </button>
                </form>
            </div>
        </div>
    </div>
</div>
